<view class="h2w h2w-{{nodes.theme}}">
	<view class="h2w__main" user-select="true">
		<decode wx:if="{{changeDecode && nodes.id}}" openTyper="{{openTyper}}" speed="{{speed}}" nodes="{{nodes}}"/>
		<decode wx:if="{{!changeDecode && nodes.id}}" openTyper="{{openTyper}}" speed="{{speed}}" nodes="{{nodes}}"/>
	</view>
</view>